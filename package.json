{
  "name": "demo-kg-monorepo",
  "version": "0.0.0",
  "packageManager": "npm@10.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "turbo": "^2.6.1"
  }
}