{
    "name": "backend",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "dev": "python -m uvicorn main:app --reload --port 8001",
        "start": "python -m uvicorn main:app --port 8001",
        "lint": "echo 'No linting configured for python yet'"
    }
}